# Configuración de Base de Datos PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=buzon_sugerencias
DB_USER=tu_usuario
DB_PASSWORD=tu_contraseña

# Puerto del Servidor
PORT=3000

# JWT Secret (cambiar en producción)
JWT_SECRET=tu_secret_key_muy_segura_aqui

# Entorno
NODE_ENV=development

# URL del Frontend (para CORS)
FRONTEND_URL=http://localhost:5173

# Configuración de Render (se configuran automáticamente en Render)
# DATABASE_URL se proporciona automáticamente por Render
# PORT se proporciona automáticamente por Render


